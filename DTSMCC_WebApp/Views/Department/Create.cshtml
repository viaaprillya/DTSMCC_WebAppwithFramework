@model DTSMCC_WebApp.Models.CreateDepartment

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Department</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="@Model.Department.Id" class="control-label">Department ID</label>
                <input asp-for="@Model.Department.Id" class="form-control" />
                <span asp-validation-for="@Model.Department.Id" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="@Model.Department.Name" class="control-label">Department Name</label>
                <input asp-for="@Model.Department.Name" class="form-control" />
                <span asp-validation-for="@Model.Department.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="@Model.Department.Div.Name" class="control-label">Divisions</label>
                <select id="lstDivisions" asp-for="@Model.Department.Div.Name" class="form-control" asp-items="@Model.Divisions">
                    <option value="">Select Division</option>
                </select>
                <span asp-validation-for="@Model.Divisions" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
